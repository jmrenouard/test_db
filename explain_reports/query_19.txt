QUERY:
SELECT e.first_name, e.last_name FROM employees e WHERE EXISTS (SELECT 1 FROM dept_emp de WHERE de.emp_no = e.emp_no GROUP BY de.emp_no HAVING COUNT(*) = 1) LIMIT 100

EXPLAIN PLAN:
id	select_type	table	type	possible_keys	key	key_len	ref	rows	Extra
1	PRIMARY	e	ALL	NULL	NULL	NULL	NULL	299202	Using where
2	DEPENDENT SUBQUERY	de	ref	PRIMARY	PRIMARY	4	employees.e.emp_no	1	

ANALYSIS ISSUES:
- Full Table Scan (ALL) detected.
