QUERY:
SELECT e.first_name, e.last_name FROM employees e JOIN dept_manager dm ON e.emp_no = dm.emp_no WHERE dm.to_date = '9999-01-01' AND e.gender = 'F'

EXPLAIN PLAN:
id	select_type	table	type	possible_keys	key	key_len	ref	rows	Extra
1	SIMPLE	dm	ALL	PRIMARY	NULL	NULL	NULL	24	Using where
1	SIMPLE	e	eq_ref	PRIMARY	PRIMARY	4	employees.dm.emp_no	1	Using where

ANALYSIS ISSUES:
- Full Table Scan (ALL) detected.
