# Project: test_db
# High Quality MariaDB Testing Environment Requirements

# System-level dependencies
# These tools must be installed on the host system
docker >= 20.10.0
docker-compose-plugin >= 2.0.0
mariadb-client >= 11.0.0
sysbench >= 1.0.20
bash >= 5.0
make >= 4.0

# Python environment
python >= 3.8
faker >= 18.0.0


# Data Files
# - employees dataset: included in /employees folder
# - sakila dataset: included in /sakila folder
